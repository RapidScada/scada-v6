@page "{id:int?}"
@model TableViewModel
@{
    Layout = "_BasicLayout";
    ViewBag.Title = "TableView";
}

@if (Model.ViewError)
{
    <div id="divError" class="box-center fs-2 text-danger">
        @Model.ErrorMessage
    </div>
}
else
{
    @Model.RenderTableView()
}
